diff --git a/node_modules/@twick/studio/dist/index.js b/node_modules/@twick/studio/dist/index.js
index f5fe21a..d555fcc 100644
--- a/node_modules/@twick/studio/dist/index.js
+++ b/node_modules/@twick/studio/dist/index.js
@@ -713,18 +713,20 @@ const StudioHeader = ({
   onExportVideo
 }) => {
   const [orientation, setOrientation] = react.useState(
-    "vertical"
+    "horizontal"
   );
   react.useEffect(() => {
     const orientation2 = localStorage.getItem("orientation");
-    if (orientation2) {
+    if (orientation2 === "horizontal") {
       setOrientation(orientation2);
     }
   }, []);
-  const handleOrientationChange = (nextOrientation) => {
+  const handleOrientationChange = async (nextOrientation) => {
     if (nextOrientation === orientation) return;
     const confirmMessage = "Changing orientation will create a new project with the new resolution. Do you want to continue?";
-    if (!window.confirm(confirmMessage)) {
+    const twickConfirm = typeof window !== "undefined" ? window.__twickConfirm : void 0;
+    const confirmed = twickConfirm ? await twickConfirm(confirmMessage) : window.confirm(confirmMessage);
+    if (!confirmed) {
       return;
     }
     onNewProject();
@@ -742,7 +744,7 @@ const StudioHeader = ({
   return /* @__PURE__ */ jsxRuntime.jsxs("header", { className: "header", children: [
     /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex-container", children: [
       /* @__PURE__ */ jsxRuntime.jsx(Clapperboard, { className: "icon-lg accent-purple" }),
-      /* @__PURE__ */ jsxRuntime.jsx("h1", { className: "text-gradient", children: "Twick Studio" }),
+      /* @__PURE__ */ jsxRuntime.jsx("h1", { className: "text-gradient", children: "Creta Studio" }),
       /* @__PURE__ */ jsxRuntime.jsx("div", { className: "header-separator" }),
       /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex-container", style: { gap: "0.5rem" }, children: [
         /* @__PURE__ */ jsxRuntime.jsx("span", { className: "text-sm opacity-80", children: "Orientation" }),
diff --git a/node_modules/@twick/studio/dist/index.mjs b/node_modules/@twick/studio/dist/index.mjs
index c21d2c0..de28502 100644
--- a/node_modules/@twick/studio/dist/index.mjs
+++ b/node_modules/@twick/studio/dist/index.mjs
@@ -714,18 +714,20 @@ const StudioHeader = ({
   onExportVideo
 }) => {
   const [orientation, setOrientation] = useState(
-    "vertical"
+    "horizontal"
   );
   useEffect(() => {
     const orientation2 = localStorage.getItem("orientation");
-    if (orientation2) {
+    if (orientation2 === "horizontal") {
       setOrientation(orientation2);
     }
   }, []);
-  const handleOrientationChange = (nextOrientation) => {
+  const handleOrientationChange = async (nextOrientation) => {
     if (nextOrientation === orientation) return;
     const confirmMessage = "Changing orientation will create a new project with the new resolution. Do you want to continue?";
-    if (!window.confirm(confirmMessage)) {
+    const twickConfirm = typeof window !== "undefined" ? window.__twickConfirm : void 0;
+    const confirmed = twickConfirm ? await twickConfirm(confirmMessage) : window.confirm(confirmMessage);
+    if (!confirmed) {
       return;
     }
     onNewProject();
@@ -743,7 +745,7 @@ const StudioHeader = ({
   return /* @__PURE__ */ jsxs("header", { className: "header", children: [
     /* @__PURE__ */ jsxs("div", { className: "flex-container", children: [
       /* @__PURE__ */ jsx(Clapperboard, { className: "icon-lg accent-purple" }),
-      /* @__PURE__ */ jsx("h1", { className: "text-gradient", children: "Twick Studio" }),
+      /* @__PURE__ */ jsx("h1", { className: "text-gradient", children: "Creta Studio" }),
       /* @__PURE__ */ jsx("div", { className: "header-separator" }),
       /* @__PURE__ */ jsxs("div", { className: "flex-container", style: { gap: "0.5rem" }, children: [
         /* @__PURE__ */ jsx("span", { className: "text-sm opacity-80", children: "Orientation" }),
